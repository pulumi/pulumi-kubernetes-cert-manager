{
    "name": "@pulumi/kubernetes-cert-manager",
    "version": "${VERSION}",
    "keywords": [
        "pulumi",
        "kubernetes",
        "cert-manager",
        "kind/component",
        "category/infrastructure"
    ],
    "scripts": {
        "build": "tsc",
        "install": "node scripts/install-pulumi-plugin.js resource kubernetes-cert-manager ${VERSION}"
    },
    "dependencies": {
        "@pulumi/kubernetes": "^3.7.1"
    },
    "devDependencies": {
        "typescript": "^3.7.0"
    },
    "peerDependencies": {
        "@pulumi/pulumi": "latest"
    },
    "pulumi": {
        "resource": true
    }
}
